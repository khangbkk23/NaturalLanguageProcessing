data:
  batch_size: 8
  max_len: 50

model:
  enc_emb_dim: 128
  dec_emb_dim: 128
  enc_hid_dim: 128
  dec_hid_dim: 128
  enc_dropout: 0.3
  dec_dropout: 0.3

training:
  n_epochs: 8
  teacher_forcing_ratio: 0.3
  learning_rate: 0.001
  clip: 1.0
